# Core CV / ML
opencv-python==4.10.0.84
face_recognition==1.3.0
dlib==20.0.1       # face_recognition dependency (may need cmake + build tools to install)
numpy==1.26.4

# Networking
requests==2.32.3

# Mock backend (standalone testing only)
Flask==3.0.3

# Standard library modules used (no install needed, listed for clarity):
# datetime, logging, json, threading, os, time, dataclasses, typing